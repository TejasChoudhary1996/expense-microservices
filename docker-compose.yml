version: "3.8"

services:

  discovery-server:
    build:
      context: .
      dockerfile: discovery-server/discovery-server/Dockerfile
    container_name: discovery-server
    ports:
      - "8761:8761"

  api-gateway:
    build:
      context: .
      dockerfile: api-gateway/api-gateway/Dockerfile
    container_name: api-gateway
    ports:
      - "8085:8085"
    depends_on:
      - discovery-server

  auth-service:
    build:
      context: .
      dockerfile: auth-service/Dockerfile
    container_name: auth-service
    ports:
      - "8082:8082"
    depends_on:
      - discovery-server
      - mysql

  expense-service:
    build:
      context: .
      dockerfile: expense-service/Dockerfile
    container_name: expense-service
    ports:
      - "8081:8081"
    depends_on:
      - discovery-server
      - mysql

  analytics-service:
    build:
      context: .
      dockerfile: analytics-service/Dockerfile
    container_name: analytics-service
    ports:
      - "8083:8083"
    depends_on:
      - discovery-server
      - api-gateway

  ui-service:
    build:
      context: .
      dockerfile: ui-service/Dockerfile
    container_name: ui-service
    ports:
      - "8080:8080"
    depends_on:
      - api-gateway

  mysql:
    image: mysql:8
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: expense_db
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: